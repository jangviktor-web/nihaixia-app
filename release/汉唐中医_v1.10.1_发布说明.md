# 汉唐中医 v1.10.1 发布说明

**发布日期**：2026-08-16
**版本号**：1.10.1（versionCode=8 / versionName=1.10.1）
**构建产物**：汉唐中医_v1.10.1.apk（约 54 MB，Android 5.0+）

---

## 本轮背景

基于「达尔文 skill」对汉唐中医 App **全量针灸穴位板块**的实证审计（对标《人纪·针灸·倪海厦》8029 行文档），识别三类问题并全量修复（P0→P3）：

1. 缺穴（督/任/肝/胆 等经脉计数缺口，6 处确证缺穴）
2. 介绍不详细（禁忌 100% 空、针/灸法 87% 空、简介均值 9.4 字、16 条经脉标注空）
3. 穴位与针灸方剂关联断裂（处方错字 / 异名未归一，解析率 90%）

审计与修复均遵循达尔文 skill 纪律：**脚本实抽 + 源码二次核验，不靠印象，人在回路**。

---

## 修复内容

### P0 — 数据质量（meridian 标注 + 处方错字）
- 补全 16 条空 `meridian` 标注（中风八大穴→经外奇穴、五里→足厥阴肝经、八髎→足太阳膀胱经、渊腋→足少阳胆经、膏肓→足太阳膀胱经、瘛脉→手少阳三焦经、禾髎→手阳明大肠经、通谷→足太阳膀胱经 等）。
- 清理 16 类处方错别字（`大抒→大杼`、`塹堂/膳中→膻中`、`蛧沟→蠡沟`、`郙门→郄门`、`巨阶→巨阙`、`脿/肺/肾俭→脾/肺/肾俞`、`大敛→大陵`、`中渺→中渚`、`十宠→十宣`、`灸至阴→至阴`、`翨风→翳风`、`肩廓→肩髃`、`肩醹→肩井`）。
- `acupoints.json` 总数 366 → 379。

### P1 — 缺穴补全
- 补 29 条缺穴：督脉 8（中枢/陶道/脑户/强间/顖会/神庭/兑端/龈交）、任脉 7（曲骨/建里/中庭/玉堂/紫宫/华盖/璇玑）、肝经 4（中封/阴廉/急脉/期门）、胆经 4（中渎/足临泣/侠溪/足窍阴）、奇穴 6（百虫窝/四缝/夜尿点/子宫/安眠/颈夹脊）。
- 14 经脉全部达标/超额，379 → 408。

### P2 — 薄条目回填（doc 抽取，仅填空不伪造）
- description 98%(401/408)、needling 55%、moxibustion 51%、contraindication 32%（讲座未讲针/灸/禁之穴如实留空）。

### P3 — 关联打通
- 穴位详情页（`acupoint_detail_screen.dart`）新增三个可点按跳转区块：**倪师处方公式**（解析临床心悟「XX方：A+B+C」）、**关联穴位处方**（反向列出含本穴的处方）、**关联透针透穴**（反向列出含本穴的透针）。
- `acupoint_repository.dart` 新增 `canonicalOf` 别名归一（处方错名/异名稳健匹配，遵循神农本草经修复铁律）。
- 修处方错字 `中阳`→`中脘`（胃痛/急性胃痛主穴），穴位↔处方关联解析率 **90% → 100%**。

### 新增 — 更新日志与更新弹窗
- 新增 `assets/data/changelog.json` + `changelog_repository.dart`：App「关于」页与「更新后弹窗」的唯一数据源。
- 「关于」页（`app_dialogs.dart`）新增更新日志展示（最新版本高亮 + 历史卡片）。
- 新增 `whats_new_service.dart`：利用 `DatabaseHelper.user_settings` 记录 `last_seen_version`，App 启动若检测到版本更新则弹出「本次更新了什么」（`home_screen.dart` 启动挂接）。
- **版本号对外展示改为 `1.10.1 / 1.10.2 / 1.10.3 …` 格式**（每次发布自增第三位 patch；内部 build 号 `+N` 同时自增作为 Android versionCode）。

---

## 代码与数据变更文件
- `lib/screens/acupoint_detail_screen.dart`：三区块 + 组成穴可点按
- `lib/data/acupoint_repository.dart`：`canonicalOf` 别名归一 + `allNames`
- `lib/screens/acupuncture_screen.dart`：处方/透针 chip 经 `canonicalOf` 稳健跳转
- `lib/screens/app_dialogs.dart`：「关于」页更新日志
- `lib/services/whats_new_service.dart`：更新后弹窗
- `lib/data/changelog_repository.dart` + `assets/data/changelog.json`：更新日志数据源
- `lib/screens/home_screen.dart`：启动挂接 whats-new
- `lib/data/database_helper.dart`：`getSetting` / `setSetting` 辅助
- `lib/main.dart`：启动加载 `ChangelogRepository`
- `assets/data/acupuncture.json`：清理 16 类错别字（备份 `acupuncture.json.bak`）
- `pubspec.yaml`：`version: 1.10.0+7 → 1.10.1+8`

## 验证结果
- `flutter analyze`：0 错误
- `flutter test`（全量）：**174/174 通过**
- `aapt2` 校验 APK：`versionCode=8 / versionName=1.10.1` ✅

## 下载
- 分享链接：https://8e56c8c253164bdd871493bcde8bee00.app.workbuddy.link
- 本地产物：`release/汉唐中医_v1.10.1.apk`

> 本应用仅供中医学习参考，不替代执业医师诊断。
