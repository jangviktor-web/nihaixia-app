# 汉唐中医 · 发布说明与更新日志（Release Notes & Changelog）

> 本文件为项目对外发布的规范发布说明（Release Notes）与完整更新日志（Changelog）。
> 版本号遵循语义化版本规范 `主版本.次版本.修订号`（如 `1.11.8`），内部 `build` 号 `+N` 作为 Android `versionCode` 单调递增。
> 每条发布记录包含：**✨ 新增（Added）· 🔧 改进（Changed/Improved）· 🐛 修复（Fixed）· ⚠️ 已知问题（Known Issues）**，按版本号倒序排列，历史清晰可追溯。

---

## [1.11.12] · 2026-09-04 — 本草一键通 · 正文药名可点 · 详情页三级结构

> **一句话** 这一版让「药」的索引彻底打通：药库列表/搜索每行都能点进详情，医案和闭门课正文里的药材名（含异名）点一下直达药物页；药物详情页的关联内容不再一页堆叠，改成「入口按钮 → 简洁列表 → 详情」三级结构。

### ✨ 新增（Added）

- **正文药名可点**：医案详情自由文本字段（病机/治法/倪师观点等）改用 `FormulaRichText`，药材/方剂名可点；闭门课 markdown 正文注入 `herb://` 链接（方剂+药材+异名合并正则，长度优先防误跳），点击经 `getExactByName` 归一后跳药物详情页。
- **详情页相关内容三级结构**：新增三个列表页 `HerbRelatedCasesScreen` / `HerbRelatedCriticalScreen` / `HerbRelatedFormulasScreen`，药物详情页只留三个入口按钮（关联医案/关联闭门课/含此药方剂，带数量徽标，医案计数异步就绪前显示 …），各入口进入仅标题的简洁列表，再点条目进对应详情页。各层级独立加载，生附子页不再一次堆 145 条医案。

### 🔧 改进（Changed/Improved）

- **本草列表/搜索全部可点击**：移除 `hasDetailedInfo` 门控（原「三项皆空的药材整行不可点」），每行均可进药物详情页。

### 🐛 修复（Fixed）

- **收藏态异步崩溃隐患**：`HerbDetailScreen` / `FormulaDetailScreen` 的 `_checkBookmark`/`_toggleBookmark` 异步回调补 `mounted` 守卫，消除 widget 已 dispose 后 `setState` 抛错（快速返回上一页即触发）。
- **出包质量**：versionCode 单调递增修正（`+0` 会被 Flutter 落成 versionCode=1，低于已装版 3，覆盖安装被拒；改为 +4）。

### ⚠️ 已知问题（Known Issues）

- 无。

---

## [1.11.11] · 2026-09-03 — 同药异名合并 · 异名检索修复 · 三盘合参与八字补全

> **一句话** 药库做了一次「同名归一」：18 组异名同种条目合并成一条正名条目（479→461），并修复合并后 74 个旧名搜不到的回归；民俗参考侧落地三盘合参与八字补全。

### ✨ 新增（Added）

- **同药异名合并**：18 组异名同种条目删除只留正名（479→461），`herb_repository.dart` `_canonicalOf` 登记 103 条「异名→正名」映射；名称与别名统一经归一检索。
- **三盘合参 B 方案**：紫微 + 八字 + 命卦同源合参；晚子时归次日改为可开关（默认按当日早子时处理）。
- **八字补全**：十神逐柱 + 旬空（空亡）、刑冲合害/合会关系检测、长生十二神双口径（火土同宫/水土同宫）用户自选。

### 🔧 改进（Changed/Improved）

- **UI 统一状态组件 `StateView`**（loading/empty/error）：10 处加载与空状态统一、3 处静默加载接入错误+重试；设计 Token 体系（间距/圆角/字号）落地，消除硬编码色值。
- **CI 签名**：接入 canonical 签名密钥 `KEYSTORE_BASE64`，GitHub Actions 四架构 APK 与本地同签名出包。

### 🐛 修复（Fixed）

- **异名检索失效**：合并后 74 个异名在药库页搜不到（如按「茈胡」检索命中不了「柴胡」）；检索逻辑收敛到唯一入口 `matchesQuery`，全量异名与正名均可命中（`tool/audit_alias_search.dart` 审计基线）。

### ⚠️ 已知问题（Known Issues）

- 无。

---

## [1.11.10] · 2026-08-31 — 命盘库 · 节气养生板块 · 黄历手势 · 大运对宫解析

> **一句话** 这一版把紫微斗数的「随身化」往前推了一步：生辰能存进「我的命盘库」随时回看；节气养生从零散的本草推荐，变成一块 24 节气独立板块，每条都附倪师节气解析；每日黄历可以手指一滑翻日翻月；大运空宫不再只丢一句"借对宫"，而是直接把对宫星情摆出来。四项都是离线、可测、标注为民俗文化参考，不替医疗下判断。

### ✨ 新增（Added）

- **我的命盘库**：排盘页新增「添加到命盘库」按钮（`Icons.bookmark_add_outlined`），可命名收藏；命盘库页（`SavedChartsScreen`）列表展示、左滑删除、点击回看——回看时回填生辰与城市重新排盘。数据库新增 `user_charts` 表（仅存生辰与地点，不持久化整盘）。用户价值：常用命盘一键收藏、跨会话回看，省去反复输入。
- **节气养生独立板块**：新建 `SolarTermSectionScreen`，顶部当前节气卡（节气名 + 距下一节气倒计时 + 养生要点 + 倪师解析），下方 24 节气竖向列表，每项卡片显示健康知识、倪师解析可展开。内容源自新建 `assets/data/solar_term_knowledge.json`（24 节气养生常识 + 基于倪海厦先生公开讲座原则整理的解析，凡非逐字原文均标注【推断】），统一免责语「倪师原话请以《人纪》《天纪》等著作为准」。并**移除旧版本草推荐联动**，健康知识改用节气 JSON，与本草库解耦。
- **每日黄历滑动手势**：`ListView` 包 `GestureDetector`，纯函数 `resolveSwipe(dx,dy)` 判定——横向占优→切月（左滑下月 / 右滑上月），纵向超阈值→切日（上滑下一天 / 下滑上一天），抖动返回 null；新增 `_shiftMonth` 跨年处理，顶部加手势提示，保留日期选择器兜底。用户价值：单手翻看黄历更顺手。

### 🔧 改进（Changed/Improved）

- **大运无主星→附对宫解析**：`ziwei_interpretation.dart` 新增 `_oppositePalaceNote`，大限空宫时于逐限评语后追加对宫（相隔六宫）主星与煞星提示，对宫亦空则注明。用户价值：紫微「借星安宫」惯例可视化，空宫大限一眼看清对宫星情。

### 🐛 修复（Fixed）

- 数据库版本升级（4→5）同时补齐 `onCreate` 建表，避免全新安装（首装即 version 5）漏建 `user_charts` 表。

### 验证（Verification）

- 新增 `test/ziwei_opposite_palace_test.dart`、`test/swipe_resolve_test.dart`、`test/saved_chart_repository_test.dart`；更新 `test/solar_term_service_test.dart`（适配移除 `seasonNature` + 新增 `getSolarTermKnowledge`）。`flutter analyze` 目标 0 新增 error；全量 `flutter test` 覆盖上述新增用例。

---

## [1.11.9] · 2026-08-29 — 让条文说人话：患者口语语料接入辨证开方

> **一句话** 新获《人纪》治症公式语料里最珍贵的，不是又添了多少首方，而是第一次系统记下了「患者自己会怎么说」。这一版把这份大白话请进了 App——既摊在结果页上给你逐句对照，也让引擎头一回听得懂现代症状词，顺带根治了「勾了怕风怕冷，却关联不到任何方剂」的老毛病。

### 一、缘起：一份语料，与一次"值不值得训练模型"的冷静权衡

这一版的种子，来自一批新到手的《人纪》经方治症公式文档——伤寒 113 方 v2、金匮 125 方 v3、本草导读 v3，外加一份结构化的 `RAG-Matrix-DB.json`。

拿到手的第一反应自然是"又有新方子了"。可真把 238 首新方与 App 里已有的 322 首逐一交叉比对，答案却出人意料：**真正净新增的，只有约 20 首**。数量上的惊喜落了空，另一处细节却让人停住目光——这批语料为每一首方都记下了一个特殊字段 `patient_oral`，也就是**患者自己会用大白话怎么描述这场病**。

于是面前摆开两条路：拿这 238 条语料去微调一个"经方小模型"，还是老老实实做成检索与展示的语料？答案选了后者——238 条样本对微调而言实在太少，硬训只会得到一个会一本正经胡说的模型；而本 App 的辨证引擎是**确定性**的，它的可信度恰恰来自"每一步都可追溯"。结论是：**不训练，做检索**。让口语语料既成为给人看的翻译，也成为给引擎用的兜底。

### 二、新能力：「患者会怎么说」——把条文译回床头那句话

《伤寒论》写"太阳中风，阳浮而阴弱"，写"啬啬恶寒，淅淅恶风"，字字精确；可对初识经方的人，终究隔着一千八百年的文言。而病榻边真实发生的，是另一种语言：

> **桂枝汤** ——「我好像感冒了，身上有点发烧，虽然一直微微出汗，但风一吹过来就觉得冷，特别怕风。鼻子塞塞的，胃里还空空的直犯干呕。」
>
> **麻黄汤** ——「我感冒得很厉害，全身上下骨头关节都在酸痛，像被鞭子痛打过一样！非常怕冷，盖好几条被子还是发抖。而且皮肤干巴巴的，一滴汗都发不出来。」
>
> **四逆汤** ——「我感觉拉肚子拉出来的都是没消化的食物……手脚冰冷到手肘和膝盖、像冰棒一样、脉搏细微得几乎摸不到了……身体里面结冰、表面却像火烧。」

本版为 **170 首方**补齐了这样的口语画像（数据源自伤寒 113 方 v2 / 金匮 125 方 v3 / `RAG-Matrix-DB.json`，已统一转简体），每一条都同时奉上四样东西：患者口语长句 `oral`、辨证指针 `indicators`、核心治法 `treatment`，以及可回溯的原文条文 `sourceText`。

在辨证结果页，这些内容化身为一张「**患者会怎么说**」卡片（`OralHintCard`）。它把大白话与标准辨证结论并排摆在一起，你可以逐句问自己——"这话，像不像我说的？" **可解释性**由此从一句口号，变成一次可以自己核对的阅读。

### 三、改进：让引擎听得懂现代话

口语语料的价值不止于"给人看"，它同时治好了一个藏得很深的毛病。

原先的方剂相似度排序（`getSimilarityRanking`）只做字面双向子串匹配，比对字段是 `keywords` / `indication` / `name` / `alias`——全是古文。于是当你在问诊里勾选"怕冷""怕风"这类**现代症状词**时，它们与"啬啬恶寒""淅淅恶风"在字面上毫无交集，得分直接归零，关联不到任何方剂。这正是此前"疾病栏 / 治法栏关联不到医案"的同一根因。

本版新增 `FormulaMatcher`，在原有基线匹配之上叠加口语语料的加权命中：**辨证指针 `indicators` 权重 0.4，患者口语 `oral` 权重 0.15**。语料先按「、」与句读切分成可匹配的最小片段再逐片段比对；更要紧的是它懂得**剔除否定**——"无便秘"不会被误算成"便秘"命中。

### 四、一道不肯让步的底线：只许"捞回"，不许"抢位"

给引擎加权重是有风险的：万一新权重把原本正确的排序搅乱，那就不是改进，是破坏。

所以这里立了一条**非劣化不变式**：口语增强分上限 `enhancementCap = 0.9`，严格小于 1。这个数字背后是一道朴素算术——既然增强分永远追不上一个完整的基础命中，那么**基线得分更高的方剂就永远排在前面**。新语料只被允许做两件事：把原本 0 命中、被漏掉的方"捞回来"，以及在基线分相同的同层之内重排先后。它不许抢任何人的位置。

### 五、修复与验证

**修复** 现代症状词（怕冷 / 怕风等）字面打不中古文条文、相似度归零、关联不到方剂与医案的历史问题——经口语语料兜底后已可有效召回。

**验证**
- 新增 `test/engine/formula_matcher_test.dart`，**7 项全部通过**，含一道关键断言：桂枝汤面对"特别怕风"这类现代词，改前 `baseline = 0`（打不中），改后 `enhancement > 0`（捞得回）；非劣化不变式另有专门用例守护。
- `flutter analyze`：本次新增代码零 error、零 warning。
- 全量 `flutter test`：**195 通过 / 3 失败**（3 项失败为既有无关模块 `minggua` / `neijing_knowledge` / `ziwei_reference`，与本次改动零交集）。

**⚠️ 已知问题** `lib/data/chinese_convert.dart` 的繁简映射表仍有缺口（缺 調→调、醬→酱、膠→胶、藶→苈 等），运行时遇到含这些字的方剂，中文匹配会失效。本次已以离线补充表规避，源表修复留待后续。

### 六、同一周期的工程改进：把 APK 交给 GitHub 自动构建发布

这一版还顺手把发版这件苦差事自动化了。新增 `.github/workflows/build-apk.yml`：今后只要推送一个 `v*` 开头的 tag（例如 `git tag v1.11.9 && git push origin v1.11.9`），GitHub Actions 便会自动编译并把四个安装包发布到同名 Release——**通用版 60.6MB**、**arm64-v8a 22.7MB**、**armeabi-v7a 20.6MB**、**x86_64 24.1MB**，全程约 6 分钟，不再需要任何人守在电脑前手动打包上传。

> 附注：本条目为叙述体长文；其余历史版本沿用「✨ 新增 · 🔧 改进 · 🐛 修复」条目体，两种体例并存，以本版为最新范式。

---

## [1.11.8] · 2026-08-19 — 六经辨证问诊增强

> **一句话**：基于对照评估补齐问诊四项增强——健康人基线判定、寒热真假八维法、六经公式分型速查，并修复用药铁律展示时机。

**✨ 新增**
- **健康人基线判定**：倪师六条健康基线（一觉到天亮 / 晨起排便 / 小便淡黄 / 手脚温 / 有胃口 / 精神好）全达标时，直接给出正面反馈「你是正常人」+ 六条逐条解读 + 养生建议，**不进入异常辨证**。
- **寒热真假八维法（选填）**：9 条显式线索（面红如妆 / 渴饮即消 / 渴不欲饮喜热饮 / 小便清长 / 小便短赤 / 胸腹久按不蒸手 / 胸腹久按蒸热 / 大便稀溏无灼热 / 大便硬结灼热），问诊后可多选、可跳过，强化真寒假热 / 真热假寒鉴别。
- **六经公式分型速查表**：新增 `meridian_formula_types.dart`，覆盖 6 经 × 分型（太阳：中风/伤寒/温病；阳明：经热/腑热；少阳：主证；太阴：脾虚；少阴：寒化/热化；厥阴：寒热错杂/血虚寒厥），含核心症状、脉象、治法、代表方、辨证要点。

**🔧 改进**
- 诊断结果详细模式按主证经渲染「六经公式分型」速查（提纲 + 分型表 + 辨证要点）。

**🐛 修复**
- **用药铁律展示时机**：用药铁律（7 大禁忌 + 5 大误治急救）与汗法禁忌原为「详细模式」专属——安全信息不应被模式开关隐藏，已上移至简单 / 详细模式均展示；顺带清理 `emergencyTreatment != null` 恒真告警。

**✅ 验证**
- `dart analyze lib`：0 error；
- 新增 `test/questionnaire_enhancement_test.dart` 5 项全过（基线 ×2、寒热真假 ×2、分型数据完整性 ×1）；
- 全量 `flutter test`：191 通过 / 3 失败（3 失败为既有无关模块 minggua / neijing_knowledge / ziwei_reference，与本次零交集）。

**⚠️ 已知问题**
- 八字详批、部分命理参考模块偶有测试红灯，属既有遗留，与医疗辨证引擎无关。

---

## [1.11.7] · 2026-08-19 — 根治医案库「治法」筛选 0 结果

> **一句话**：真机点治法 chip 显示"共 0 / 1113 例"。根因是 `FormulaRepository` 与页面加载的竞态 + 简繁字面不一致，三件套修复彻底消除。

**🐛 修复**
- `FormulaRepository.load()` 改为**幂等**（已加载则跳过），消除重复解析与竞态写入；
- `MedicalCaseLibraryScreen._load()` 显式 `await` 方剂库与药材库加载完成（不依赖 App 启动顺序，双保险）；
- 筛选函数 `filterMedicalCases` 的 formula / disease 分支改用 `toSimplified` **双侧繁简归一**比较，消除简繁字面不一致。

**✅ 验证**
- 端到端回归测试：点击「桂枝汤」→ 28 例、「糖尿病」→ 38 例断言通过；
- `dart analyze` 0 error。

---

## [1.11.6] · 2026-08-19 — 医案列表卡片新增西医病名徽标

> **一句话**：点「疾病栏」筛出的医案，卡片上即可看到命中的西医病名，无需进详情。

**✨ 新增**
- 医案列表卡片显示**西医病名徽标**（最多 3 个，超出折叠为 `+N`），命中关系一目了然。

**✅ 验证**
- 筛选结果与各 chip 频次完全一致（乳癌 112 / 肺癌 82 / 肝癌 63 …），golden 回归测试覆盖新布局。

---

## [1.11.5] · 2026-08-19 — 修复「治法 / 疾病」筛选栏不显示

> **一句话**：V1.11.4 新增的两栏此前异步计算，部分时机下不显示。改为同步计算，首帧即有完整筛选行。

**🐛 修复**
- 分类频次由后台异步改为 `_load()` 内**同步计算**，确保首帧渲染「年份 / 治法 / 疾病 / 视图」四行；
- 新增端到端渲染回归测试。

---

## [1.11.4] · 2026-08-19 — 医案库筛选增强

> **一句话**：筛选栏从两行升级为四行，且每行更懂医理。

**✨ 新增**
- **治法栏改按经方方剂名**分类（取高频前 12，如四逆汤 35 / 桂枝汤 28 / 当归四逆汤 27 …）；
- **新增疾病栏**按西医病名分类（乳癌 112 / 肺癌 82 / 肝癌 63 / 糖尿病 38 …）；
- **西医病名词典**：约 150 个正名 + 简繁别名归一（乳腺癌→乳癌、前列腺癌→攝護腺癌）。

---

## [1.11.3] · 2026-08-19 — 医案列表白字热修

> **一句话**：修复浅色模式下医案卡片文字变白、不可读的致命问题。

**🐛 修复**
- 列表卡片与详情页 `RichText` 全部显式绑定语义色（onSurface / onSurfaceVariant）；
- 新增 **golden 截图回归测试**，把"文字必须可读"写进自动化测试。

---

## [1.11.2] · 2026-08-19 — 医案详情秒开

> **一句话**：打开医案详情偶发约 1 秒卡顿，根源是首帧同步扫描全库索引。

**🔧 改进**
- 列表加载后**后台分块预热**方剂 / 药材索引缓存（60 例一块，块间让出帧）；
- 详情页「相关医案」改为**首帧后异步计算**，正文立即可见。

---

## [1.11.1] · 2026-08-19 — 交互体验修复 · 图标规范 · 设计 Token

> **一句话**：基于三方 UI 审计整改，让每个按钮都可靠、每处文字都规范。

**🐛 修复**
- 底部导航保状态（Tab 切换不丢进度）；
- App 内更新安装修复（正确拉起系统安装器）；
- 命理日期校验（2 月不再出现 30/31 日）；
- 聊天「返回上一步」修复；
- 六经详情死按钮补入口；
- 剂量换算崩溃封堵。

**🔧 改进**
- 全站 128 处功能 emoji 图标统一替换为 **Material Icons**；
- 新增 **39 个语义色 Token**，硬编码颜色清零；
- 深色模式全面修复（六经 / 状态色自动切换亮档）；
- 字号收敛到 6 级。

---

## [1.11.0] · 2026-08-18 — 黄帝内经模块 + 附子生炮拆分 + 命理三件套

> **一句话**：人纪五部在 App 内全部就位；附子按倪师体系拆出两条独立条目；命理工具补流年盘、六爻摇卦、八字详批。

**✨ 新增**
- **黄帝内经**：脏象 12 卡 + 望诊（五色 / 眼诊）+ 脉诊（平人 / 死脉）+ 73 篇阅读库 + 全文搜索；
- **倪师医案库 1257 例**检索 + 闭门课重症临床模块，医案 ↔ 方剂双向联动；
- **命理三件套**：紫微流年盘、六爻铜钱摇卦、八字详批。

**🔧 改进**
- **附子生炮拆分**：生附子 / 炮附子独立成条，方剂组成 1663 处引用零死链；
- 紫微增强：杂星 37 个十二神全中文、支持真太阳时 + 经度。

---

## [1.10.3] · 2026-08-17 — 七步问诊十问模块 v3.1 改版

> **一句话**：以倪师经方口径升级十问题库与信号链路，修正吴茱萸汤巅顶痛误判。

**✨ 新增 / 改进**
- 脉象 13→15 项、疼痛 10→20 项、新增呕吐类型追问（Q12）；
- 21 处新症状信号接入分经路由；
- 吴茱萸汤按「巅顶痛」路由（原误用后脑痛）。

**✅ 验证**：strict 准确性 harness 23/23 通过；全量 `flutter test` 205/2。

---

## [1.10.2] · 2026-08-16 — 紫微斗数排盘集成 + 跨引擎精度验证

**✨ 新增**：实用工具页新增紫微斗数排盘（基于 `ziwei_core`，纯离线）。
**🐛 修复**：改用平太阳时，修复真太阳时导致时辰位移的精度 bug。
**✅ 验证**：MingLi-Bench 跨引擎比对核心字段 32/32 对齐；全量 182/182 通过。

---

## [1.10.1] · 2026-08-16 — 针灸穴位板块审计修复 + 更新日志与弹窗

**✨ 新增**
- 穴位详情页「关联打通」：倪师处方公式 / 关联穴位处方 / 关联透针透穴，均可点按跳转；
- 新增 `changelog.json` + 「关于」页更新日志 + 「更新后弹窗」。

**🐛 修复**
- 穴位总数 379→**408**，归属经脉 0 空标注，穴位↔处方关联 90%→**100%**；
- 修正 17 类处方错别字（大杼→大抒、膻中→塹堂 等）。

---

## [1.10.0] · 2026-08-16 — 本草经第四轮核查 + 七步问诊重构

> 本版本历经 +4 ~ +7 多次迭代（七步问诊按键审计、病证→方关联核查三轮、神农本草经补全），关键成果：

**✨ 新增**
- 神农本草经别名归一，组成可解析率 67%→**91%**；
- 补建 8 味《本经》漏载药（440→448 味）；
- 七步问诊引擎重构：以六经为纲 + 金匮 19 证候族鉴别树，257 首方剂可达。

**🐛 修复**
- 5 处「病证→方」关联修正（狐惑主方应为甘草泻心汤、杏子汤实为麻黄杏仁甘草汤、桃红四物汤归桂枝茯苓丸等）；
- 引擎 P0-P3 缺陷（fever 未赋值、精确串误判、顺序遮蔽等）修复，方剂可达性 226/226（100%）。

---

## [1.9.1] · 2026-08-16 — 七步问诊引擎 P0-P3 缺陷修复

**🐛 修复**：引擎缺陷确证并修复（fever 赋值解封 5 方、沉脉改写太阳修复、证据闸白名单扩充等 10 项）。
**✅ 验证**：引擎 226/226 可达（100%）；`flutter test` 174/174。

---

## [1.5.0] · 2026-06-12 — 设置面板全面升级

**✨ 新增**
- 默认性别、诊断详细度、自动复制处方；
- 清除历史 / 导出收藏 / 清理缓存；
- 关于页面 + GitHub 检测更新。

---

## [1.4.0] · 应用内更新检测（GitHub Releases API）

**✨ 新增**：应用内一键检测新版本 APK。

---

## [1.3.0] · 完整处方生成与诊断增强

**✨ 新增**
- 完整处方生成（组成 + 剂量 + 煎服法 + 禁忌 + 加减建议）；
- 鉴别诊断扩展（26 对）、合病扩展（14 种）、脉舌矛盾警告、真寒假热 / 真热假寒鉴别、用药铁律与汗法禁忌、传经预警。

---

## [1.2.0] · 诊断历史与速查增强

**✨ 新增**：诊断历史趋势图（六经传变可视化）、方剂 / 药物速查增强（搜索 + 筛选）。

---

## [1.1.0] · 实用工具起步

**✨ 新增**：子午流注取穴计算器、经方剂量换算器。

---

## [1.0.0] · 初始版本

**✨ 新增**
- 六经辨证智能诊断；
- 方剂速查（271 首）、药物速查（345 味）、穴位数据（361 穴）。

---

<p align="center">
完整的工程级变更记录（含文件级改动与验证基线）见根目录 <a href="CHANGELOG.md">CHANGELOG.md</a>。
</p>
