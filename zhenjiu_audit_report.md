# 汉唐中医 App · 全量针灸穴位板块 实证核查报告

> 方法论：**达尔文 skill 纪律** —— 脚本实抽（Python 解析文档与 JSON）+ 源码二次核验（`acupoint_repository.dart` / `acupoint_detail_screen.dart` / `acupuncture.json`），不凭印象；计数推断部分显式标注「待文档段落确认」。
> 审计日期：2026-08-16 ｜ 当前版本：v1.10.0+7 ｜ 数据源：《人纪·针灸·倪海厦》（8029 行 / 417K 字）+ `assets/data/acupoints.json`(379) / `acupuncture.json`

---

## 0. 方法与数据源

| 项目 | 内容 |
|------|------|
| 权威基准 | 《人纪·针灸·倪海厦》文档，14 经脉章节逐穴讲授（任督 + 十二正经） |
| App 主数据 | `assets/data/acupoints.json`（声明 total=366，实际列表 379 条） |
| App 处方数据 | `assets/data/acupuncture.json`：`acupuncture.categories`（14 类穴位处方）+ `penetration`（31 条透针） |
| 关联代码 | `lib/data/acupoint_repository.dart`（`findByName` 精确+去「穴」匹配）、`acupoint_detail_screen.dart`、`acupuncture_screen.dart`（「穴位处方」Tab） |
| 比对手法 | ① 抽取文档逐穴标题 → 穴位词表；② 与 app 名集合 diff；③ 逐条回查文档/源码排除解析噪声与异体字；④ 统计穴位处方引用名解析率 |

---

## 1. 总体结论

| 问题类别 | 量化结论 | 严重度 |
|----------|----------|--------|
| **一、缺穴** | 文档点名而 app 全缺：**6 处已确认**；另按经脉计数缺口约 **24 处**（待文档段落确认） | 中 |
| **二、薄条目** | `contraindication` 100% 空；`needling`/`moxibustion` 87% 空；`description` 均值仅 **9.4 字**；16 条 meridian 字段为空 | **高** |
| **三、关联断裂** | 穴位处方引用解析率 **90%（216/240）**，24 处断裂 = 真实缺穴 7 + 处方文件错别字 17；详情页未打通关联 | 中 |

---

## 2. 问题一：缺穴（文档讲授但 app 缺载）

### 2.1 已确认缺载（文档标题点名、app 全无，源码二次核验通过）

| 穴名 | 归属经脉 | 文档依据 | 重要性 |
|------|----------|----------|--------|
| **期门** | 足厥阴肝经（肝之募穴） | 文档「期门，脾为章门，肾为京门…」 | 高（募穴，临床常用） |
| **兑端** | 督脉 | 「素髎。兑端。龈交穴」 | 中 |
| **龈交** | 督脉 | 同上 | 中 |
| **陶道** | 督脉 | 「大椎与陶道穴」 | 中 |
| **顖会**（=囟会） | 督脉 | 「前顶。顖会①与上星穴」 | 中 |
| **百虫窝** | 经外奇穴 | 「百虫窝[…]」 | 中（治虫积/皮肤） |

### 2.2 按经脉计数缺口（对照标准经穴数；文档均覆盖该经脉，故为缺穴强信号）

> ⚠️ 以下为「app 计数 < 标准经穴数」的推断，候选缺穴名单需**逐穴回查文档段落最终确认**（达尔文纪律：不凭计数下结论）。

| 经脉 | app | 标准 | 缺口 | 候选缺穴（待确认） |
|------|-----|------|------|--------------------|
| 督脉 | 21 | 28 | −7 | 兑端、陶道、龈交、顖会 + 脑户/强间/神庭等 |
| 任脉 | 17 | 24 | −7 | 待回查文档确认具体穴名 |
| 肝经 | 9 | 14 | −5 | 期门、中封、足五里、阴廉、急脉 |
| 胆经 | 41 | 44 | −3 | 侠溪、足临泣 + 1（见 §4.1 真缺穴） |
| 三焦经 | 22 | 23 | −1 | 待确认 |
| 膀胱经 | 66 | 67 | −1 | 待确认 |

> 说明：2.1 的 6 处已 100% 确认；2.2 中「肝经 −5」的具体穴名由 app 列表与标准经穴列表 diff 得出（实证，非猜测），侠溪/足临泣经 §4.1 处方引用佐证。

---

## 3. 问题二：介绍不详细（薄条目）

字段填充率（n=379）：

| 字段 | 空/薄 | 占比 | 说明 |
|------|-------|------|------|
| `contraindication` 禁忌 | **379 空** | **100%** | 无一穴有禁忌提示 |
| `needling` 针刺 | 329 空 | 87% | 仅 50 穴有内容 |
| `moxibustion` 灸法 | 329 空 | 87% | 仅 50 穴有内容 |
| `description` 简介 | 378 条 <40 字 | 99.7% | 均值 **9.4 字**，多为「调理X、治Y」一句话 |
| `location` 位置 | 0 空 | 0% | ✅ 唯一填得好的字段 |
| `clinicalNotes` 临床心悟 | 379 充实 | — | 均值 332 字，134 穴含针灸方（见 §4.2） |

**结论**：除「位置」与「临床心悟」外，其余字段严重缺失。对针灸学习者而言，针刺深浅、灸法、禁忌恰是最该有的操作信息，目前几乎全空。

### 3.1 数据质量 Bug

- **16 条 `meridian` 字段为空**（丢失经脉归类）：
  `中风八大穴、五里穴、八髎穴、反关脉穴、和髎穴、四关穴、天地人三皇穴、条口透承山穴、治肝三穴、消积穴、渊腋穴、瘛脉穴、禾髎穴、耳尖穴、膏肓穴、通谷穴`
  其中 **渊腋(胆)、膏肓(膀胱)、瘛脉(三焦)、禾髎(大肠/三焦)、通谷(膀胱/肾)、五里(肝/大肠)** 本为标准经穴却未标注经脉 → 按经脉浏览时丢失、且 §4 关联断裂。
- `acupoints.json` 顶部 `total: 366`，实际列表 **379** → 元数据不一致（+13）。

---

## 4. 问题三：与针灸方剂关联断裂

### 4.1 穴位处方引用解析率 **90%（216/240 唯一引用名可解析）**，24 处断裂

`acupuncture.json` 14 类处方共 756 条引用、240 个唯一穴名。断裂分两类：

**(a) 真实缺穴**（处方引用了、但 app 无此穴 —— 同时是问题一的延伸）：
`侠溪、足临泣、四缝、夜尿点、子宫、安眠、颈夹脊`

**(b) 处方文件错别字**（穴在 app 有，但处方里写错名 → `findByName` 失败、跳转空白）：

| 正确名 | 处方错写 | 正确名 | 处方错写 |
|--------|----------|--------|----------|
| 大杼 | 大抒 | 膻中 | 塹堂 / 膳中 |
| 蠡沟 | 蛧沟 | 郄门 | 郙门 |
| 巨阙 | 巨阶 | 脾俞/肺俞/肾俞 | 脿俭/肺俭/肾俭 |
| 大陵 | 大敛 | 中渚 | 中渺 |
| 十宣 | 十宠 | 至阴 | 灸至阴 |
| 翳风 | 翨风 | 肩髃/肩井 | 肩廓/肩醹 |

### 4.2 详情页未打通关联（有数据、无链接）

- `acupoint_detail_screen.dart` 仅把 `clinicalNotes` 当**纯文本**展示。其中 **134 穴** 含有「XX方：A + B + C + D」形式的针灸方，共交叉引用 **192 个不同穴位**，但**无任何超链接**跳转到组成穴，也**没有反向「哪些处方用到此穴」列表**。
- 即：丰富的关联数据躺在 JSON 里，UI 层完全没用起来 —— 类比此前神农本草经 67%→91% 的关联修复，此处属于「有数据、无链接」。

### 4.3 透针（penetration）关联

- 31 条透针（中府透云门、阳陵泉透阴陵泉…），`source` 字段引用大量穴名，但透针 `name` 本身是组合名（「中府透云门」），`findByName` 无法解析，组合名也无对应详情页 → 透针关联基本悬空。

---

## 5. 修复建议（待用户确认后执行）

沿用既定流程：**验证 → 升版本 → 构建 → 部署 → 邮件**

| 优先级 | 任务 | 要点 |
|--------|------|------|
| **P0 数据质量** | 补 16 条 meridian 标注；`total` 改 379；清理 `acupuncture.json` 17 处错别字 | 系统性、零风险 |
| **P1 缺穴** | 补建 §2.1 六穴；回查文档确认 §2.2 候选并补建；补 四缝/夜尿点/子宫/安眠/夹脊 等奇穴 | 先确认再补 |
| **P2 薄条目** | 回填 `contraindication`/`needling`/`moxibustion`（文档对各穴有针法灸法禁忌讲解，可结构化抽取）；扩 `description` | 文档粒度有限，矿物/奇穴类据实、不伪造 |
| **P3 关联** | ① 穴位详情页增「相关穴位处方」区块（解析 clinicalNotes 方+组成穴，可点按跳转）；② 穴位处方页错名归一（`canonicalOf` 思路，类比 herb 修复）；③ 透针组合名建映射 | 复用神农本草经经验 |

**验证基线**：`flutter analyze` 0 错 + `flutter test` 全过 + `aapt2` 校验 `versionCode`。

---

## 6. 达尔文纪律声明

本报告所有数字均来自脚本实抽 + 源码二次核验；「缺穴」中计数推断部分已显式标注「待文档段落确认」，未凭印象下结论。**人在回路**：报告产出后等待用户确认，再动手修复。
